# シフト管理アプリ 開発まとめ

## 概要

**Dandy Kitchen（キッチンカー）向け** シフト管理Webアプリ。  
管理者（Google認証）とバイトスタッフ（URLのみ・ログイン不要）の2ロール構成。

- **本番URL**: https://dandy-shift-manager.netlify.app
- **リポジトリ**: https://github.com/ogmanage/DandyDays-shift-manager
- **データ保存先**: Googleスプレッドシート（ID: `1slGLRdNpbKjZIGH769dBiaWN6pgF8a9EeeybeHtKjw4`）

---

## 技術スタック

| 分類 | 技術 |
|------|------|
| フロントエンド | React 18 + TypeScript + Vite + Tailwind CSS |
| 認証 | Google Identity Services（GIS）OAuth 2.0 |
| DB | Google Sheets API（管理者側）|
| スタッフAPI | Google Apps Script（GAS）Web App |
| ホスティング | Netlify |
| ルーティング | React Router v6（HashRouter）|

---

## 構成ファイル

```
app/src/
├── pages/
│   ├── admin/
│   │   ├── Login.tsx           # Googleログイン画面
│   │   ├── Dashboard.tsx       # URL共有・GAS設定
│   │   ├── ShiftManagement.tsx # シフト枠管理・カレンダー
│   │   └── StaffManagement.tsx # スタッフ管理
│   └── staff/
│       └── StaffResponse.tsx   # バイト回答画面（ログイン不要）
├── services/
│   ├── googleAuth.ts           # GIS OAuth実装
│   ├── sheetsService.ts        # Sheets API読み書き
│   └── gasService.ts           # GAS URL管理
├── store/
│   └── useStore.ts             # 全状態管理（楽観的更新）
└── components/
    ├── Layout.tsx              # 管理者共通レイアウト
    ├── Modal.tsx
    └── Badge.tsx
```

---

## 実装機能

### 管理者側
- Googleログイン・セッション保持
- シフト月の作成・公開・締め切り
- シフト枠（日付・場所・必要人数）追加・編集・削除・月コピー
- バイト向けURLのワンクリック共有
- シフト回答の一覧・確定処理
- カレンダー表示（月グリッド）
- スタッフ管理（追加・編集・権限変更・削除）
- スプレッドシート再接続UI

### バイト側
- URLを開くだけ（ログイン不要）
- 日程・場所ごとの参加可否を選択して送信
- 前回回答の自動読み込み

---

## アーキテクチャの要点

### データフロー
```
管理者 → Google OAuth → Sheets API → スプレッドシート
バイト → GAS Web App URL → スプレッドシート
```

### 楽観的更新
UIを先に更新してからバックグラウンドでSheetsに同期。  
ただし**管理者権限変更だけは同期を待ってからUI更新**（別ユーザーのログイン時に権限が反映されないバグを防ぐため）。

### 状態管理
- データ：`localStorage`（端末キャッシュ）
- ログイン中のadminId：`sessionStorage`
- スプレッドシートID：`localStorage` or 環境変数`VITE_SPREADSHEET_ID`

---

## ハマったポイントと解決策

| 問題 | 原因 | 解決 |
|------|------|------|
| Googleログインが「ログイン中」で止まる | GISの`error_callback`未設定 | `error_callback` + `pendingReject`パターン追加 |
| GitHub PagesでOAuthがブロック | COOP headerを変更できない | **Netlifyに移行**（`_headers`で`same-origin-allow-popups`設定） |
| 管理者昇格が反映されない | `syncToSheets`がfire-and-forget | `updateMemberRole`を`async`化して書き込み確認後にUI更新 |
| 新管理者がセットアップ画面に飛ぶ | Sheets未共有 + 環境変数未反映 | スプレッドシートを共有 + `VITE_SPREADSHEET_ID`をNetlifyに設定 |
| iOS/Androidでヘッダーがズレる | safe area未対応 | `env(safe-area-inset-top/bottom)`を各所に追加 |
| スマホで印刷ボタンが反応しない | `window.print()`がモバイル非対応 | スマホ時はスクリーンショット案内に切り替え |

---

## Netlify設定

```toml
# netlify.toml
[build]
  base = "app"
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

```
# app/public/_headers
/*
  Cross-Origin-Opener-Policy: same-origin-allow-popups
```

**環境変数（Netlify管理画面で設定）:**
- `VITE_GOOGLE_CLIENT_ID` = Google CloudのOAuth Client ID
- `VITE_SPREADSHEET_ID` = 使用するスプレッドシートID

> ⚠️ 環境変数を変更したら「Retry without cache with latest branch commit」で強制再ビルド必須

---

## 運用上の注意

| 項目 | 内容 |
|------|------|
| Googleログイン制限 | OAuthアプリがテストモードのため**テストユーザー登録必須** |
| テストユーザー追加場所 | Google Cloud Console → APIとサービス → OAuth同意画面 → テストユーザー |
| トークン失効 | テストモードでは7日で失効。月1利用なら実質影響なし |
| スプレッドシート共有 | 管理者全員のGoogleアカウントに「編集者」権限が必要 |
| 新管理者追加の手順 | ①Google Cloudにテストユーザー追加 → ②Sheetsを共有 → ③アプリのスタッフ管理でrole=adminに昇格 |

---

## 他案件への転用ポイント

### そのまま使えるケース
- スタッフがGmailを持っていて、管理者が少人数で自分で運用する小規模シフト管理
- クライアントごとに別Google Cloudプロジェクト＋別Sheetsで完全分離して販売

### スケールしたくなったら
- **Google審査を通す**: プライバシーポリシーページ＋申請で本番公開 → 7日失効なし・テストユーザー上限なし
- **Service Account方式**: Google OAuth廃止 → パスワード認証 + サービスアカウントでSheets読み書き → 管理者にGmailアドレス不要・再ログイン不要

### 注意点
- Google OAuth（GIS）を使う場合はNetlifyかCloudflare Pagesなど**COOPヘッダーをカスタムできるホスティング**が必須（GitHub Pagesは不可）
- `sticky`ヘッダーはモバイルで不安定なことがある → `height: 100dvh; overflow-y: auto`の親コンテナ内で使うと確実
